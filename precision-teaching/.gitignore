__pycache__/
# Pupil data now lives in its own private repo (wallscourtfarm/
# precision-teaching-data), nested here as a plain git checkout with its
# own .git — never tracked by staff-tools itself.
private-data/
